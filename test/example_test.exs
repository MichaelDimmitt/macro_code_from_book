defmodule ExampleTest do
  use ExUnit.Case
  # doctest Example

  # require Example
  test "ch #, assert example" do
    assert 1 === 1
  end

  test "ch #, refute example" do
    assert 1 === 1
  end

end
